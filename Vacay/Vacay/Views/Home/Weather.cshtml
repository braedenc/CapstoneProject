@model Vacay.Models.WeatherViewModels
@{
    ViewBag.Title = "Weather";
}

<h2>Weather</h2>

<div class="row">
    <div class="col-md-4">
        <div class="row">
            @*@Html.LabelFor(m => m.City, new { @class = "col-md-2 control-label" })*@
            City:
        </div>
        <div class="row">
            @Html.TextBoxFor(m => m.City, new { @class = "form-control" })
        </div>
    </div>
    <div class="col-md-4">
        <div class="row">
            @*@Html.LabelFor(m => m.StartDate, new { @class = "col-md-2 control-label" })*@
            Start Date of your vacation: (DD/MM/YYYY)
        </div>
        <div class="row">
            @Html.TextBoxFor(m => m.StartDate, new { @class = "form-control" })
        </div>
    </div>
    <div class="col-md-4">
        <div class="row">
            @*@Html.LabelFor(m => m.EndDate, new { @class = "col-md-2 control-label" })*@
            End Date: (DD/MM/YYYY)
        </div>
        <div class="row">
            @Html.TextBoxFor(m => m.EndDate, new { @class = "form-control" })
        </div>
    </div>
</div>
<div class="row">
    <div class="form-group">
        <input type="submit" class="btn btn-default" value="Find Weather" />
    </div>
</div>

@*<div>
    <div class="row">
        <button class="btn btn-default" id="weatherButton" onclick="weatherAPI()">What's the weather like today?</button>
        <br>
        <br>
        <div id="weather"></div>
        <div id="temperature"></div>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script>
                                            function weatherAPI() {
                                                var showWeather = $("#weather");
                                                var showTemp = $("#temperature");
                                                var zipCode = 94114;
                                                var apiKey = '64b0083fb0c4d9a010c1608d34bb37f5';
                                                var location = "http://api.openweathermap.org/data/2.5/forecast/daily?zip=" + zipCode + "&APPID=" + apiKey + "&units=imperial";

                                                $.ajax({
                                                    url: location,
                                                    method: "GET",
                                                    dataType: "jsonp",
                                                    success: function (data) {
                                                        var high = data.list[0].temp.max;
                                                        var low = data.list[0].temp.min;
                                                        showWeather.empty();
                                                        showTemp.empty();

                                                        showWeather.append(data.list[0].weather[0].main + " in " + data.city.name);
                                                        showTemp.append("High:" + high + " " + "Low:" + low);
                                                    }
                                                });
                                            }
    </script>



</div>*@

